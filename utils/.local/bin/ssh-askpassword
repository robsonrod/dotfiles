#!/usr/bin/env bash

password=$(whiptail --passwordbox "Please enter your secret password" 8 78 --title "Password" 3>&1 1>&2 2>&3)
exitstatus=$?
if [ $exitstatus != 0 ]; then
    echo "User selected Cancel."
else
    echo "$password"
fi

